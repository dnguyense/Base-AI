---
description: Hướng dẫn AI xử lý yêu cầu tích hợp APK từ người dùng
globs: "**/*.{apk,smali,xml}"
alwaysApply: false
---

# Xử Lý Yêu Cầu Tích Hợp APK
description: Hướng dẫn AI cách xử lý yêu cầu tích hợp APK từ người dùng
globs: "**/*.{apk,smali,xml}"

## Nhận Diện Yêu Cầu Tích Hợp
Khi người dùng yêu cầu tích hợp APK, thường sẽ có dạng:
- "Tích hợp [package] từ [APK nguồn] vào [APK đích]"
- "Copy package [tên package] từ [APK A] sang [APK B]"
- "Thêm module [tên module] vào [APK đích]"

## Quy Trình Thu Thập Thông Tin
Khi nhận yêu cầu tích hợp APK, ***PHẢI*** thu thập đầy đủ các thông tin sau bằng cách hỏi tuần tự:

### 1. Hỏi APK nguồn
Nếu người dùng chưa cung cấp APK nguồn:
```
Bạn vui lòng cung cấp đường dẫn đến file APK nguồn (chứa package cần tích hợp)?
```

### 2. Hỏi APK đích
Nếu người dùng chưa cung cấp APK đích:
```
Đường dẫn đến file APK đích (APK cần được tích hợp package vào) là gì?
```

### 3. Hỏi Package Path
Nếu người dùng chưa cung cấp package:
```
Package hoặc class path bạn muốn tích hợp là gì? (ví dụ: com/miui hoặc com/example/feature)
```

### 4. Xác nhận thông tin
Trước khi thực hiện, ***PHẢI*** xác nhận lại thông tin:
```
Tôi sẽ thực hiện tích hợp với các thông tin sau:
- APK nguồn: [đường_dẫn_APK_nguồn]
- APK đích: [đường_dẫn_APK_đích]
- Package cần tích hợp: [package_path]

Bạn xác nhận là chính xác chưa?
```

## Các Bước Thực Hiện
Sau khi đã thu thập và xác nhận đầy đủ thông tin, AI sẽ thực hiện tuần tự các bước sau, luôn ***GIẢI THÍCH*** từng bước đang thực hiện:

### Bước 1: Tạo thư mục làm việc
```bash
# Tạo thư mục làm việc với timestamp
TIMESTAMP=$(date +"%Y%m%d_%H%M%S")
WORK_DIR="apk_integration_${TIMESTAMP}"
mkdir -p "${WORK_DIR}"/{source,target,output}
```

### Bước 2: Copy APK vào thư mục làm việc
```bash
cp "[đường_dẫn_APK_nguồn]" "${WORK_DIR}/source/source.apk"
cp "[đường_dẫn_APK_đích]" "${WORK_DIR}/target/target.apk"
```

### Bước 3: Decompile APK nguồn
```bash
apktool d "${WORK_DIR}/source/source.apk" -o "${WORK_DIR}/source/decompiled" -f
```

### Bước 4: Decompile APK đích
```bash
apktool d "${WORK_DIR}/target/target.apk" -o "${WORK_DIR}/target/decompiled" -f
```

### Bước 5: Kiểm tra package trong APK nguồn
```bash
# Tìm package trong các thư mục smali
SOURCE_PACKAGE=""
for smali_dir in $(find "${WORK_DIR}/source/decompiled" -type d -name "smali*"); do
  if [ -d "${smali_dir}/[package_path]" ]; then
    SOURCE_PACKAGE="${smali_dir}/[package_path]"
    break
  fi
done

# Nếu không tìm thấy, liệt kê các package có sẵn
if [ -z "$SOURCE_PACKAGE" ]; then
  echo "Không tìm thấy package [package_path] trong APK nguồn"
  echo "Các package có sẵn trong APK nguồn:"
  for smali_dir in $(find "${WORK_DIR}/source/decompiled" -type d -name "smali*"); do
    find "$smali_dir" -mindepth 1 -maxdepth 3 -type d | sed "s|$smali_dir/||g" | sort | uniq | head -n 10
  done
  exit 1
fi
```

### Bước 6: Phân tích cấu trúc APK đích
```bash
# Tìm các thư mục smali trong APK đích
TARGET_SMALI_DIRS=()
for smali_dir in $(find "${WORK_DIR}/target/decompiled" -type d -name "smali*"); do
  TARGET_SMALI_DIRS+=("$smali_dir")
done

# Chọn thư mục smali phù hợp hoặc tạo mới
if [ ${#TARGET_SMALI_DIRS[@]} -eq 0 ]; then
  DEST_SMALI="${WORK_DIR}/target/decompiled/smali"
  mkdir -p "$DEST_SMALI"
else
  DEST_SMALI="${TARGET_SMALI_DIRS[0]}"
fi
```

### Bước 7: Tích hợp package
```bash
# Tạo thư mục cha nếu cần
mkdir -p "$(dirname "${DEST_SMALI}/[package_path]")"

# Copy package
cp -r "${SOURCE_PACKAGE}" "$(dirname "${DEST_SMALI}/[package_path]")"
```

### Bước 8: Kiểm tra AndroidManifest.xml
```bash
PACKAGE_DOT=$(echo "[package_path]" | tr '/' '.')
if grep -q "$PACKAGE_DOT" "${WORK_DIR}/source/decompiled/AndroidManifest.xml"; then
  echo "Package xuất hiện trong AndroidManifest.xml của APK nguồn"
  echo "Bạn có thể cần cập nhật AndroidManifest.xml của APK đích"
  grep -A 3 -B 3 "$PACKAGE_DOT" "${WORK_DIR}/source/decompiled/AndroidManifest.xml"
fi
```

### Bước 9: Build lại APK
```bash
apktool b "${WORK_DIR}/target/decompiled" -o "${WORK_DIR}/output/integrated.apk"
```

## Báo Cáo Kết Quả
Sau khi hoàn thành, AI ***PHẢI*** báo cáo kết quả với thông tin:

```
✅ Tích hợp hoàn tất!

📱 APK đã tích hợp: [đường_dẫn_APK_tích_hợp]

📋 Các bước tiếp theo:
1. Ký APK với jarsigner
2. Tối ưu hóa APK với zipalign
3. Cài đặt và test

⚠️ Lưu ý: Bạn có thể cần kiểm tra và cập nhật AndroidManifest.xml
```

## Xử Lý Lỗi
AI ***PHẢI*** xử lý các lỗi phổ biến và cung cấp thông tin hữu ích:

### Lỗi không tìm thấy APK
```
❌ File APK '[đường_dẫn]' không tồn tại!
Vui lòng kiểm tra lại đường dẫn đến file APK.
```

### Lỗi không tìm thấy package
```
❌ Không tìm thấy package '[package_path]' trong APK nguồn!

Các package có sẵn trong APK nguồn:
[danh_sách_packages]

Vui lòng chọn package từ danh sách trên hoặc kiểm tra lại tên package.
```

### Lỗi khi build APK
```
❌ Gặp lỗi khi build APK!

Chi tiết lỗi:
[log_lỗi]

Có thể do:
1. Xung đột giữa các class
2. Phiên bản API không tương thích
3. Thiếu resources cần thiết

Bạn có muốn:
1. Kiểm tra log chi tiết?
2. Thử một package khác?
```

## Trường Hợp Đặc Biệt

### Package cần resources
Nếu package có thể yêu cầu resources, AI ***PHẢI*** cảnh báo và hỏi người dùng:
```
⚠️ Package này có thể yêu cầu resources (hình ảnh, layout, v.v.).
Bạn có muốn tôi cũng sao chép các resources liên quan không?
```

Nếu người dùng đồng ý:
```bash
# Tìm các resources liên quan
RESOURCE_PREFIX=$(echo "[package_path]" | tr '/' '.' | cut -d '.' -f1-2)
mkdir -p "${WORK_DIR}/resources_related"
grep -r "$RESOURCE_PREFIX" "${WORK_DIR}/source/decompiled/res" --include="*.xml" -l | xargs -I{} cp {} "${WORK_DIR}/resources_related/"
```

## Ví Dụ Tương Tác
Dưới đây là ví dụ tương tác mẫu khi AI nhận yêu cầu tích hợp APK:

**Người dùng**: Tôi muốn tích hợp module camera từ APK Xiaomi vào ứng dụng của tôi

**AI**:
```
Tôi sẽ giúp bạn tích hợp module camera. Vui lòng cung cấp các thông tin sau:

Bạn vui lòng cung cấp đường dẫn đến file APK nguồn (APK chứa module camera)?
```

**Người dùng**: /Users/trungkientn/Downloads/MiuiCamera.apk

**AI**:
```
Cảm ơn bạn. Đường dẫn đến file APK đích (APK của ứng dụng bạn) là gì?
```

**Người dùng**: /Users/trungkientn/MyApp/app/release/app-release.apk

**AI**:
```
Package hoặc class path của module camera bạn muốn tích hợp là gì? (ví dụ: com/xiaomi/camera)
```

**Người dùng**: com/xiaomi/camera

**AI**:
```
Tôi sẽ thực hiện tích hợp với các thông tin sau:
- APK nguồn: /Users/trungkientn/Downloads/MiuiCamera.apk
- APK đích: /Users/trungkientn/MyApp/app/release/app-release.apk
- Package cần tích hợp: com/xiaomi/camera

Bạn xác nhận là chính xác chứ?
``` 